{
  "posts": [
    { "id": 1, "title": "json-server", "author": "typicode" }
  ],
  "comments": [
    { "id": 1, "body": "some comment", "postId": 1 }
  ],
  "profile": { "name": "typicode" }
}

const call = (() => {
  console.log('nims');
  fetch('shinobi.json', {mode: 'no-cors'})
  .then(res => res.json())
  .then(data => console.warn(data))
  .catch((error) => console.warn(error));
})();